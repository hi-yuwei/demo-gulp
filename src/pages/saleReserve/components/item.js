/* Vue.component('card', {
  props: {
    list: {
      //列表内容
      type: Array,
      default: [1, 2, 3, 4]
    }
  },
  template: `
        <div>
          <ul>
            <li v-for="(item,index) in list" :key="index">
              {{item}}
            </li>
          </ul>
          
        </div>
    `,
  methods: {
    okHandle() {
      this.$emit('ok')
    },
    cancelHandle() {
      this.$emit('cancel')
    }
  }
})
 */
